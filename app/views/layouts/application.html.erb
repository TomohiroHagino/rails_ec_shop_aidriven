<!DOCTYPE html>
<html>
  <head>
    <title>ECショップ - DDD Architecture</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= yield :javascript %>
  </head>

  <body>
    <header class="header">
      <nav>
        <div class="header__container">
          <div class="header__logo-section">
            <%= link_to "ECショップ", root_path, class: "header__logo" %>
            <span class="header__divider">|</span>
            <%= link_to "商品一覧", products_path, class: "header__nav-link" %>
          </div>
          <div class="header__nav-section">
            <% if logged_in? %>
              <%= link_to "カート", cart_items_path, class: "header__nav-link" %>
              <%= link_to "注文履歴", orders_path, class: "header__nav-link" %>
              <span class="header__user-name"><%= current_user.name %> さん</span>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "header__logout-btn" %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class: "header__nav-link" %>
              <%= link_to "新規登録", new_user_registration_path, class: "header__nav-link" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main class="main">
      <% if flash[:notice] %>
        <div class="flash flash--notice">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="flash flash--alert">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
